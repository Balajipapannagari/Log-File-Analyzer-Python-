# Log-File-Analyzer-Python-
A Python program that reads server log files and extracts error counts, most frequent IP addresses, and failed login attempts using string processing and dictionaries.

# Log File Analyzer (Python)

## 📌 Project Overview
This project is a Python-based **Log File Analyzer** that reads server log files
and extracts useful insights such as error counts, failed login attempts,
and the most frequently accessing IP address.

## 🚀 Features
- Reads server log file line by line
- Counts total error occurrences
- Detects failed login attempts (401, 403)
- Identifies most frequent IP address
- Uses exception handling for safe execution

## 🧠 Skills Used
- Python file handling
- String manipulation
- Dictionaries
- Loops
- Error handling


